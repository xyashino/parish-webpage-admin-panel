export const enum RequestPath {
  OneAlbum = "/albums/",
  Albums = "/albums",
  AlbumTypes = "/albums/types",
  CurrentUser = "/users/current",
  Users = "/users",
  OneIntention = "/intentions/",
  Announcements = "/announcements/",
  Logout = "/auth/logout",
  Register='/users/register',
}
